# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/portal_db?useSSL=false&serverTimezone=Asia/Kuala_Lumpur&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=ydl11222004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis Configuration
mybatis.configuration.map-underscore-to-camel-case=true

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Email Configuration (using Gmail as an example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Server Configuration
server.port=8080

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.example.portal=DEBUG

# Time Zone Configuration
spring.jackson.time-zone=Asia/Kuala_Lumpur
